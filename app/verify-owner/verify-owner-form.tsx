"use client";
import { useState } from "react";
export function VerifyOwnerForm() { const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); const response = await fetch("/api/verification", { method: "POST", body: new FormData(event.currentTarget) }); const data = (await response.json()) as { verification?: { status: string }; error?: string }; setBusy(false); setMessage(response.ok ? "Verification request received for manual review." : data.error ?? "Unable to submit"); } return <form className="workflow-form" onSubmit={submit}><div className="form-section"><h2>Identity document</h2><p className="form-help">Image or PDF, maximum 10 MB. Access is restricted to authorized reviewers.</p><input required name="identity" type="file" accept="image/*,application/pdf" /></div><div className="form-section"><h2>Proof of property relationship</h2><p className="form-help">Upload a document supporting your owner, manager, or agency relationship. This is not automatic title certification.</p><input required name="propertyProof" type="file" accept="image/*,application/pdf" /></div><label className="consent-check"><input required name="consent" type="checkbox" /> I consent to private processing for identity and property-relationship review.</label>{message && <p className="form-message">{message}</p>}<button className="button button-primary button-block" disabled={busy}>{busy ? "Uploading securely…" : "Request manual review"}</button></form>; }
