import { Header } from "../components/Header";
import { SCORE_WEIGHTS } from "../lib/score";

export default function MethodologyPage() {
  return <main><Header locale="en" /><article className="legal-page"><span className="eyebrow">Trust methodology</span><h1>How the opportunity score works</h1><p>The score is a transparent decision aid. It is not an appraisal, safety certification, legal opinion, or guarantee that a home is suitable.</p><h2>Weighted components</h2><ul><li><strong>Price value ({SCORE_WEIGHTS.priceValue * 100}%):</strong> asking price per square meter compared with the current model reference. Early beta values are provisional until sufficient local observations exist.</li><li><strong>Visible condition ({SCORE_WEIGHTS.conditionScore * 100}%):</strong> confirmed guided-capture evidence and coverage. No inspection means lower confidence, not proof of poor condition.</li><li><strong>Listing trust ({SCORE_WEIGHTS.trustScore * 100}%):</strong> identity review, structured completeness, and consistency signals. It never labels a person a scammer.</li><li><strong>Location fit ({SCORE_WEIGHTS.locationFit * 100}%):</strong> property coordinates and user-selected places only. Demographic characteristics are never used.</li></ul><h2>Formula</h2><pre>composite = 0.30 × price + 0.25 × condition + 0.25 × trust + 0.20 × location</pre><p>Every component is clamped to 0–100 and the composite is rounded. The model version is stored with each score so later changes remain auditable.</p><h2>Current beta limitation</h2><p>Until DariRent has enough consented, deduplicated Tunisian asking and signed-rent observations, price values are directional. Preview listings are explicitly labeled and cannot be contacted.</p></article></main>;
}
