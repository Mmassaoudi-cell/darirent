import { Header } from "../components/Header";
import { SCORE_WEIGHTS } from "../lib/score";
import { MIN_COMPS_SAMPLE } from "../lib/scoring-service";

export default function MethodologyPage() {
  return <main><Header locale="en" /><article className="legal-page"><span className="eyebrow">Trust methodology</span><h1>How the opportunity score works</h1><p>The score is a transparent decision aid. It is not an appraisal, safety certification, legal opinion, or guarantee that a home is suitable.</p><h2>Weighted components</h2><ul><li><strong>Price value ({SCORE_WEIGHTS.priceValue * 100}%):</strong> asking price per square meter compared with the median of published, non-preview listings in the same neighborhood once at least {MIN_COMPS_SAMPLE} comparable listings exist.</li><li><strong>Visible condition ({SCORE_WEIGHTS.conditionScore * 100}%):</strong> confirmed guided-capture evidence and coverage. No inspection means lower confidence, not proof of poor condition.</li><li><strong>Listing trust ({SCORE_WEIGHTS.trustScore * 100}%):</strong> identity review, structured completeness, and consistency signals. It never labels a person a scammer.</li><li><strong>Location fit ({SCORE_WEIGHTS.locationFit * 100}%):</strong> property coordinates and user-selected places only. Demographic characteristics are never used.</li></ul><h2>Formula</h2><pre>composite = 0.30 × price + 0.25 × condition + 0.25 × trust + 0.20 × location</pre><p>Every component is clamped to 0–100 and the composite is rounded. The stored model version includes the reference sample size and median so later changes remain auditable.</p><h2>Current beta limitation</h2><p>Below {MIN_COMPS_SAMPLE} non-preview published listings in a neighborhood, DariRent falls back to the provisional 14 DT/m² reference. Preview listings are explicitly labeled, excluded from comparable data, and cannot be contacted.</p></article></main>;
}
